---
title: "Software"
excerpt: "well, an exerpt"
---

Well lets try to see what happens. AAAAA

{% highlight python %}
import requests
import json

payload_check_verify = {
    'emailAddress': paypal_email,
    # accountIdentifier
    'firstName': firstname,
    'lastName': lastname,
    'matchCriteria': 'NAME',  # NONE, NAME
    'requestEnvelope': {
        'errorLanguage': 'en_US',
        'detailLevel': 'ReturnAll'
    }
}

r = requests.post(PAYPAL_API_ENDPOINT_CHECK_VERIFIED, data=json.dumps(payload_check_verify), headers=PAYPAL_HEADERS)
if r.json()['responseEnvelope']['ack'] == 'Success':
    return True
else:
    return False
{% endhighlight %}